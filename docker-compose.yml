services:
  ubldigital-callcenter:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: ubl-callcenter-gemini
    ports:
      - "7019:7019"
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./recordings:/app/recordings
      - ./pages:/app/pages
    restart: unless-stopped
    healthcheck:
      test: [ "CMD", "curl", "-f", "http://localhost:7019/" ]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
